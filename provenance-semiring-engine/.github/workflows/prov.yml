name: provenance-semiring-engine

on:
  push:
    paths:
      - "provenance-semiring-engine/**"
      - ".github/workflows/prov.yml"
  pull_request:
    paths:
      - "provenance-semiring-engine/**"
      - ".github/workflows/prov.yml"

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: provenance-semiring-engine
    strategy:
      matrix:
        python: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
          cache-dependency-path: provenance-semiring-engine/pyproject.toml
      - name: Install
        run: pip install -e ".[dev]"
      - name: Lint
        run: ruff check src tests
      - name: Test
        run: pytest -q
