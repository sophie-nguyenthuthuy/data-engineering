name: api-rate-limit-orchestrator

on:
  push:
    branches: [main]
    paths: ["api-rate-limit-orchestrator/**", ".github/workflows/arlo.yml"]
  pull_request:
    paths: ["api-rate-limit-orchestrator/**", ".github/workflows/arlo.yml"]
  workflow_dispatch:

defaults:
  run:
    working-directory: api-rate-limit-orchestrator

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - run: pip install --upgrade pip && pip install -e ".[dev]"
      - run: python -m ruff check src tests
      - run: python -m ruff format --check src tests
      - run: python -m mypy src
      - run: python -m pytest tests -q

  docker:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
      - run: docker build -t arlo:ci api-rate-limit-orchestrator
      - run: docker run --rm arlo:ci info
