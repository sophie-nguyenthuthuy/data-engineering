apiVersion: 1

providers:
  - name: pipeline
    orgId: 1
    folder: Pipeline
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
